# 1. Create a virtual env (like .venv)
uv venv .venv

# 2. Activate it (PowerShell)
.\.venv\Scripts\Activate.ps1

# Or CMD
.\.venv\Scripts\activate.bat

# 3. Install dependencies from requirements.txt
uv pip install -r requirements.txt